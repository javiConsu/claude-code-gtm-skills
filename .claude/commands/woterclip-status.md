Show WoterClip status (queue, blocked issues, last heartbeat) using the woterclip-status skill.

Parse $ARGUMENTS for optional flags:
- `--history`: Show recent heartbeat history from the log file instead of current status

Run the woterclip-status skill, passing the parsed arguments.
